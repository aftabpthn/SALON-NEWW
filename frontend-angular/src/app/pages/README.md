# Pages

Route entry pages live here and must be grouped by domain.

Each page/component must use separate files:

- `*.component.ts`
- `*.component.html`
- `*.component.css`

If a page is 60% or more related to an existing domain, add it inside that domain folder.
