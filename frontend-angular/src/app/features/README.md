# Features

Put reusable domain business features here.

Use an existing domain folder when the new feature is 60% or more related to that domain.

Examples:

- appointment availability engine -> `appointments/`
- booking portal widgets -> `booking/`
- POS payment tools -> `pos/` or `payments/`
- customer profile intelligence -> `clients/`
